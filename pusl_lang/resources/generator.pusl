let range = fn(start, end):
    let i = start
    while i < end:
        yield i
        i = i + 1

let gen = range(1, 10)
while gen.hasNext():
    print(gen.next())

gen = range(1, 5)
while true:
    print(gen.next())