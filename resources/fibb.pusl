let fibbonacci = fn(num):
    if num > 2:
        return self(num-1) + self(num - 2)
    else if num == 2:
        return 1
    else:
        return 0
let c = Object()
let c.f = fibbonacci
let b = c.f(10)
print(b, "\n")
